language: csharp
solution: ./VS2013_PROJECT/NZPortalWeb.sln
sudo: enabled
mono: latest

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libclucene-contribs1 libclucene-core1 libclucene-dev

# safelist
branches:
  only:
  - master